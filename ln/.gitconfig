[alias]
	whoami = config user.email
        unstage = reset HEAD --
        graph = log --graph --decorate --pretty=oneline --abbrev-commit --color=always

[user]
        # email = 
        # name = 
        
[init]
	defaultBranch = main

[core]
        editor = vim
        excludesFile = ~/.global.gitignore
